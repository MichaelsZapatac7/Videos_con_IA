# Dependencias de la VOZ LOCAL (clonación con XTTS-v2 / Coqui TTS).
# Instalar DENTRO del entorno .venv-voz (Python 3.11), NO en el principal.
#
# IMPORTANTE: PyTorch se instala APARTE, con el índice CUDA 12.8 (Blackwell/RTX 50):
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# Luego:
#   pip install -r youtube_pipeline/requirements-voz.txt
#
# Guía completa: SETUP_VOZ_LOCAL.md

coqui-tts>=0.25.0

# Compatibilidad con torch 2.9+ (probado en torch 2.11 / RTX 5090):
# - transformers 5.x rompe XTTS (quita isin_mps_friendly) -> fijar a la rama 4.x.
# - torch 2.9+ exige torchcodec para IO de audio.
transformers>=4.57,<5
torchcodec

# F5-TTS (español): motor recomendado (mejor acento latino + naturalidad).
# Modelo: jpgallegoar/F5-Spanish (se descarga aparte a .venv-voz/f5-spanish).
f5-tts
